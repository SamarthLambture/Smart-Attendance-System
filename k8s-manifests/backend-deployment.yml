apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: attendo-ns
  labels:
    app: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend-pod
          image: ghcr.io/samarthlambture/attendo-backend:0309fd501037905ee96423b99993e0c9534aee07
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: config
            - secretRef:
                name: secret
